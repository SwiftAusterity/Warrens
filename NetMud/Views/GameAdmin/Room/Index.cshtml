@model NetMud.Models.Admin.ManageRoomDataViewModel
@{
    ViewBag.Title = "Manage Rooms";

    @Html.Partial("AddContentLink")

    if (Model.Items.Count() > 0)
    {
        @Html.Partial("SearchCriteriaAndPaging", new Tuple<NetMud.Models.IPagedDataModel, string>(Model, "Room"))

        using (Html.BeginForm("Remove", "Room"))
        {
            @Html.AntiForgeryToken()
            <table class="table">
                <thead>
                    <tr>
                        <td>
                            ID
                        </td>
                        <td>
                            Name
                        </td>
                        <td>
                            Remove
                        </td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var obj in Model.CurrentPageOfItems)
                    {
                        <tr>
                            <td>@Html.ActionLink(@obj.Id.ToString(), "Edit", new { id = obj.Id }) [@Html.ActionLink("Map", "Map", new { id = obj.Id })]</td>
                            <td>
                                @if (obj.FitnessProblems)
                                {
                                    <span style="color:red;" title="@String.Join(", ", obj.FitnessReport())">@obj.Name</span>
                                }
                                else
                                {
                                    @obj.Name
                                }
                            </td>
                            <td>
                                @Html.Partial("RemoveDataItem", @obj.Id)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }

    }
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
