@using NetMud.Models
@using NetMud.DataStructure.Linguistic

@model HashSet<IDictata>

@{
    var currentCount = Model.Count();
    var rowsName = ViewData.ModelMetadata.PropertyName + "Rows";
}

<div id="@rowsName" class="iterativeContainer">
    <div class="addRowContainer">
        <div class="col-sm-1">
            @Html.DescriptionFor(m => m)
            <a href="#" class="addRow btn" onclick="javascript: $('#@rowsName').append(newMineralList); return false;"><span class="addWord">Add Row</span></a>
        </div>
    </div>
    @if (currentCount > 0)
    {
        @Html.EditorForMany(m => m, null, 0, "CollectionDictataListItem")
    }
</div>

<script type="text/javascript">
    var newMineralList = decodeHtml('@Html.EmptyEditorForMany(m => m, null, currentCount, "CollectionDictataListItem")');
</script>
