@using NetMud.Models
@using NetMud.DataStructure.Architectural.EntityBase

@model HashSet<IQuality>

@{
    var currentCount = Model.Count();
    var rowsName = ViewData.ModelMetadata.PropertyName + "Rows";
}

<div id="@rowsName" class="iterativeContainer">
    <div class="addRowContainer">
        <div class="col-sm-1">
            @Html.DescriptionFor(m => m)
            <a href="#" class="addRow btn" data-current-row="@currentCount" onclick="javascript: replacePart(this, newProficenciesRow, @currentCount, '@rowsName'); return false;"><span class="addWord">Add Row</span></a>
        </div>
    </div>
    @if (currentCount > 0)
    {
        @Html.EditorForMany(m => m, null, 0, "TeachableProficency")
    }
</div>

<script type="text/javascript">
    var newProficenciesRow = decodeHtml('@Html.EmptyEditorForMany(m => m, null, currentCount, "TeachableProficency")');
</script>
